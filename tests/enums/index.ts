export enum statusCode {
  OK = 200,
  CREATED = 201,
  NOCONTENT = 204,
  INVALID = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOTFOUND = 404,
}

export enum userData {
  ID = 3,
  EMAIL = "eve.holt@reqres.in",
  PASSWORD = "cityslicka",
  NAME = "RONY",
  JOB = "tester",
}

export enum pageNumber {
  FIRST = 1,
  SECOND = 2,
  INVALID = 30,
}
